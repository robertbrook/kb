- @javascript = :defaults
%h1= @record.display_title

%table.record
  = render :partial => 'table_header'
  %tbody
  = render :partial => 'record', :object => @record, :locals => {:notes => @record.html_formatted_notes }

= link_to 'Edit', edit_record_path(@record)
= link_to 'Back', records_path